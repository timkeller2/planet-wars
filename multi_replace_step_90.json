{
  "name": "multi_replace_file_content",
  "args": {
    "Description": "\"Refactored getCanvasPos, getPinchMidpoint, and all mouse/touch event listeners in src/main.js to make canvas interactions pixel-perfect across scaled devices and High DPI screens.\"",
    "Instruction": "\"Update coordinate calculation and interaction helpers in src/main.js to use logical canvas pixels instead of raw CSS screen pixels, and apply them across mouse/touch handlers while scaling pan offsets.\"",
    "ReplacementChunks": "[{\"AllowMultiple\":false,\"EndLine\":68,\"ReplacementContent\":\"  function getPinchMidpoint(touches) {\\n    const rect = canvas.getBoundingClientRect();\\n    const scaleX = canvas.width / rect.width;\\n    const scaleY = canvas.height / rect.height;\\n    return {\\n      x: ((touches[0].clientX + touches[1].clientX) / 2 - rect.left) * scaleX,\\n      y: ((touches[0].clientY + touches[1].clientY) / 2 - rect.top) * scaleY\\n    };\\n  }\",\"StartLine\":63,\"TargetContent\":\"  function getPinchMidpoint(touches, rect) {\\n    return {\\n      x: (touches[0].clientX + touches[1].clientX) / 2 - rect.left,\\n      y: (touches[0].clientY + touches[1].clientY) / 2 - rect.top\\n    };\\n  }\"},{\"AllowMultiple\":false,\"EndLine\":735,\"ReplacementContent\":\"  function getCanvasPos(clientX, clientY) {\\n    const rect = canvas.getBoundingClientRect();\\n    const scaleX = canvas.width / rect.width;\\n    const scaleY = canvas.height / rect.height;\\n    return {\\n      x: (clientX - rect.left) * scaleX,\\n      y: (clientY - rect.top) * scaleY\\n    };\\n  }\",\"StartLine\":717,\"TargetContent\":\"  function getCanvasPos(event) {\\n    if (!event.target) return { x: event.clientX, y: event.clientY };\\n    const rect = event.target.getBoundingClientRect();\\n    const scaleX = canvas.width / rect.width;\\n    const scaleY = canvas.height / rect.height;\\n    \\n    // Fallback to offsetX/Y if scaling produces strange offsets on some devices\\n    if (event.offsetX !== undefined && event.offsetY !== undefined) {\\n       return {\\n         x: event.offsetX * scaleX,\\n         y: event.offsetY * scaleY\\n       };\\n    }\\n\\n    return {\\n      x: (event.clientX - rect.left) * scaleX,\\n      y: (event.clientY - rect.top) * scaleY\\n    };\\n  }\"},{\"AllowMultiple\":false,\"EndLine\":1032,\"ReplacementContent\":\"  canvas.addEventListener('mousedown', (event) => {\\n    const cPos = getCanvasPos(event.clientX, event.clientY);\\n    const posX = cPos.x;\\n    const posY = cPos.y;\",\"StartLine\":1029,\"TargetContent\":\"  canvas.addEventListener('mousedown', (event) => {\\n    const cPos = getCa\n<truncated 11205 bytes>",
    "TargetFile": "\"S:\\\\dist\\\\antigravity\\\\planet wars\\\\src\\\\main.js\"",
    "toolAction": "\"Refactoring coordinates and event handlers\"",
    "toolSummary": "\"Edit coordinates in main.js\""
  }
}